# Base URL of the deployed FastAPI backend (no trailing slash).
# CRA inlines REACT_APP_* vars at build time, so this must be set in
# Vercel's project settings (Production/Preview/Development) BEFORE building,
# not just in a runtime .env -- changing it requires a redeploy.
REACT_APP_BACKEND_URL=https://your-backend-host.example.com
